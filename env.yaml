name: eda-agent

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip

  # Core scientific stack
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - sqlalchemy>=2.0

  # Media / signal processing
  - opencv
  - librosa>=0.10
  - pysoundfile>=0.12

  # MCP + Python packages not ideal through conda
  - pip:
      - mcp